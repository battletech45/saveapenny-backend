package com.saveapenny.audit.entity;

public enum AuditEntityType {
    USER,
    ACCOUNT,
    CATEGORY,
    TRANSACTION,
    TRANSFER,
    BUDGET,
    RECURRING_TRANSACTION,
    NOTIFICATION,
    IMPORT
}
