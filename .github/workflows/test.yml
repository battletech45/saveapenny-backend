name: Test

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
    tags:
      - "v*.*.*"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: test-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tests:
    name: Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Set up Java 24 (Temurin)
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        with:
          java-version: "24"
          distribution: "temurin"
          cache: maven

      - name: Print Java version
        run: java -version

      - name: Print Maven version
        run: mvn -version

      - name: Set up OCR runtime
        uses: ./.github/actions/setup-ocr

      - name: Run tests
        run: mvn -B clean verify -Dsurefire.excludes="**/ocr/infrastructure/engine/tesseract/OcrGoldenImageRegressionTest.java,**/ocr/interfaces/http/integration/OcrImportRealMediaIntegrationTest.java"

      - name: Upload surefire reports
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: surefire-reports
          path: |
            **/target/surefire-reports/*
          if-no-files-found: warn

      - name: Upload failsafe reports
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: failsafe-reports
          path: |
            **/target/failsafe-reports/*
          if-no-files-found: warn

      - name: Upload test timing metrics
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: test-metrics
          path: |
            **/target/test-metrics/*
          if-no-files-found: warn

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: jacoco-report
          path: |
            **/target/site/jacoco/*
          if-no-files-found: warn

      - name: Write job summary
        if: always()
        run: |
          {
            echo "## Test"
            echo
            echo "- Ref: \`${{ github.ref }}\`"
            echo "- Event: \`${{ github.event_name }}\`"
            echo "- Java: Temurin 24"
            echo "- OCR runtime: tesseract-ocr, tesseract-ocr-eng"
            echo "- OCR_TESSDATA_PATH: \`${OCR_TESSDATA_PATH}\`"
            echo "- Command: \`mvn -B clean verify\`"
            echo "- Artifacts: \`surefire-reports\`, \`failsafe-reports\`, \`test-metrics\`, \`jacoco-report\`"
          } >> "$GITHUB_STEP_SUMMARY"
